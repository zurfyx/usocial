import styled from 'styled-components';
import BoxHeader from './BoxHeader';

const BoxMultilineHeader = styled(BoxHeader)`
  h2 {
    margin-top: 0.8rem;
    margin-bottom: 0.8rem;
  }
`;

export default BoxMultilineHeader;
