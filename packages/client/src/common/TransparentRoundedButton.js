import styled from 'styled-components';
import TransparentButton from './TransparentButton';

const TransparentRoundedButton = styled(TransparentButton)`
  border-radius: 25px;
`;

export default TransparentRoundedButton;
